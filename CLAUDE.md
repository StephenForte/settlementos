@AGENTS.md

# SettlementOS — project context

EVM stablecoin settlement MVP (see README.md for full docs + demo script). The PRD
lives at ~/Downloads/settlementos_evm_stablecoin_settlement_prd.md.

## State (2026-07-07)
- Phases 1–4 complete: single-chain settlement, FX/routing/compliance/liquidity,
  multi-chain demo (two local Hardhat nodes: base-local 31337 on :8545,
  polygon-local 31338 on :8546, simulated bridge), and real Base Sepolia support
  (chainId 84532, Basescan links) — DEPLOYED and verified on the real testnet
  (PaymentSettlement: 0x9d8b8b7c476ab02306046f3da719d380fa0456aa; first settled
  payment tx 0xdbf963150f5c1c90e3a007cc474c3fd42255fd3d019e3d71a6d821528fe258c5).
- Run: `npm run chain` + `npm run chain:polygon` + `npm run setup` (resets DB and
  redeploys locals, re-registers Base Sepolia wallets) + `npm run dev`.
  Base Sepolia: fund the deployer in .env, then `npm run deploy:base-sepolia`
  (see README "Base Sepolia"). Tests: none yet; verify via API curl + UI.
- Key modules: lib/networks.ts (registry + explorer URLs), lib/chain.ts (viem
  adapter; per-network accounts via accountsFor(), keys resolved inline or via
  privateKeyEnv from .env), lib/routing.ts (quotes), lib/executor.ts (lifecycle
  orchestration), lib/compliance.ts (mock providers), lib/audit.ts (hash-chained
  log), scripts/setup.mjs (local deploy + seed), scripts/deploy-base-sepolia.mjs
  (testnet deploy → chain/deployments.base-sepolia.json, gitignored).

## Base Sepolia status
- Deployed 2026-07-07. Deployer/operator 0x5128889F20Ec13e0Be38b2BeBC568594159B652d
  (key in .env), funded via Alchemy faucet; ~0.078 ETH gas remaining. Deploy cost
  ~0.0018 ETH; a settled payment costs well under 0.0001 ETH, so no refill needed
  for many demos. Re-running `npm run deploy:base-sepolia` redeploys fresh
  contracts but reuses the generated treasury/entity wallets.
- Deployer = operator; treasury + entity wallets are generated by the deploy
  script and funded with dust ETH for approvals. Never put dev-mnemonic keys or
  the mainnet key on a public network; never commit .env or chain/deployments*.json.
